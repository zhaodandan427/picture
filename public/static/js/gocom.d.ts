/**
 * Created by lichun on 2017/12/12.
 */
export declare class GoCom {
  maxWindow(isMaxWindow);
  selectUser(preSelectedUsers,isMulti,successCallback,failureCallback);
  closeWindow();
  chatWithUser(chatUserId);
 
}
